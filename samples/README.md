# Samples

This directory contains example traces and response bundles used by the testable KernelWeave prototype.

The actual sample traces are generated into the store during `kernelweave add-sample` and verified by the test suite.
